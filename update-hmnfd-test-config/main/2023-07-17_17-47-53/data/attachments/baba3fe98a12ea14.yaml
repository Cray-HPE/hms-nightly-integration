- name: Delete testing bootparameters for Compute role
  request:
    url: "{bss_base_url}/boot/v1/bootparameters"
    method: DELETE
    verify: !bool "{verify}"
    json:
      hosts:
      - Compute
  response:
    status_code:
    - 200
    - 400
    - 404
