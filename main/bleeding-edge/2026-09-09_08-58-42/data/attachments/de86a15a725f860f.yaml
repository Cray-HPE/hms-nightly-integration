- name: Remove test Bootparameter for compute node
  request:
    url: "{bss_base_url}/boot/v1/bootparameters"
    method: DELETE
    verify: !bool "{verify}"
    json:
      hosts:
      - "{node_xname}"
  response:
    status_code: 200