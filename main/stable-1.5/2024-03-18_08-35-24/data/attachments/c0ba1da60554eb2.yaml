- name: Make sure that a list of Components is returned
  request:
    url: "{hsm_base_url}/hsm/v2/State/Components"
    method: GET
    verify: !bool "{verify}"
  response:
    status_code: 200
    json:
      Components: !anything
    save:
      json:
        ID: Components[0].ID


